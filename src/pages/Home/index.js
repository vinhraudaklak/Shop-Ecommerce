import React from 'react';
import { DefaultLayout } from '~/components/Layout';

const Home = () => {
    return (
        <div>
            <DefaultLayout />
        </div>
    );
};

export default Home;
